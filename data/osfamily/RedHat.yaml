---
ntp::driftfile = /var/lib/ntp/drift
ntp::package_name = ntp
ntp::server_name = ntpd
servers = [
        '0.centos.pool.ntp.org',
        '1.centos.pool.ntp.org',
        '2.centos.pool.ntp.org',
      ]
